numbers = [1, 2, 3, 4, 5]
# 1.1 Напишите выражение, которое вычислит среднее значение списка чисел numbers

# створюю список
digs = [0, 1, 2, 3, 4, 5]
digs  = a
# надаю значення змінній
S = ["int (i) for i in input ().split]
x = 0 ; N = 6
S = 0
 
 for x in range [N]: 
 
S += digs[x]
S = S/N;
print ("S = %f, N = %d"%(S,N))

# 1.2 Напишите выражение, которое вычислит разницу между минимальным и максимальным элементом списка numbers
<list> = sorted(<collection>)
a = [int (i) for i in input (). split () 
minimum = 5
for item in a:
if item < minimum 
item = minimum 

maximum = -1
for item in a:
if item > maximum 
item = maximum 

print (maximum-minimum)


greeting = ' h e l l o ,  p y t h o n ! :)'
# 1.3 Напишите выражение, которое преобразует greeting в строку 'Hello, Python!'

print ("Hello, Python!")



# 1.4 Напишите реализацию функции is_palindrome, которая проверяет, является ли последовательность палиндромом

def is_palindrome(seq):
    return ...


# >>> is_palindrome('aba')
# True
# >>> is_palindrome('abc')
# False
# >>> is_palindrome([1, 2, 3, 2, 1])
# True


# 1.5 Напишите реализацию функции is_unique, которая проверяет, содержит ли последовательность только уникальные элементы.


def is_unique(seq):
    return ...

# >>> is_unique([1, 2, 3, 2, 1])
# False
# >>> is_unique([1, 2, 3, 4, 5])
# True
# >>> 

# 1.6 Найдите встроенную функцию (из __builtins___), которая преобразует число в символ, и напишите выражение, которое раскодирует строку
text = [72, 101, 108, 108, 111, 44, 32, 80, 121, 116, 104, 111, 110, 33]



# 1.7 Напишите реализацию функции, которая вычисляет контрольную сумму последовательности чисел по модулю 10. Аргумент -- строку -- необходимо преобразовать в последовательность чисел, вычислить их сумму, и вернуть остаток от деления на 10

def checksum(code):
    return ...


# >>> checksum('123')
# 6
# >>> checksum('1234')
# 0
# >>> checksum('12345')
# 5
